# Uncomment the next line to define a global platform for your project
platform :ios, '10.2'

pod 'Cluster', '~> 1.0'

target 'FindMyPups' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  
  # Pods for FindMyPups

  target 'FindMyPupsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FindMyPupsUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'Alamofire'
  pod 'SwiftyJSON'

end
